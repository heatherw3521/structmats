function A = blockbuilder(H,Hparent)
% builds Hblock
% H is the piece we wish to reconstruct
% Hparent is the parent of H

if H.isleaf
    A = leafbuild(H);
end


if H = Hparent.A12
    left = leftdrill(Hparent.A11);
    right = rightdrill(H, Hparent);
else
    left = leftdrill(H, Hparent);
    right = rightdrill(H, Hparent);
end


A = left * H.Z * H.lrcomponent * H.Y * right;


end

function left = leftdrill(H,Hparent)

if H.isleaf

end

end